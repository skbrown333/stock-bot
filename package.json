{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.0.7",
    "lerna": "^3.15.0",
    "typescript": "^3.5.1"
  },
  "scripts": {
    "publish": "lerna run tsc && lerna publish"
  }
}
